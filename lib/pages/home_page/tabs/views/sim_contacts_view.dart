import 'package:flutter/material.dart';

class SimContactsView extends StatelessWidget {
  const SimContactsView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('SIM Contacts'),
    );
  }
}
