import 'package:flutter/material.dart';

class EmailContactsView extends StatelessWidget {
  const EmailContactsView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('SIM Contacts'),
    );
  }
}
